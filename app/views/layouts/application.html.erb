<!DOCTYPE html>
<html>
  <head>
    <title>PictureSNS</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body> 
    <header>
      <ul>
        <li><%= link_to "投稿一覧", root_path %></li>
      <% if logged_in? %>
        <li><%= link_to "新規投稿", new_picture_path %></li>
        <li><%= link_to "お気に入り一覧", favorite_index_path(current_user.id) %></li>
        <li><%= link_to "プロフィール", user_path(current_user.id) %></li>
        <li><%= link_to "ログアウト", session_path(current_user.id), method: :delete %></li>
      <% else %>
        <li><%= link_to "ユーザ登録", new_user_path %></li>
        <li><%= link_to "ログイン", new_session_path %></li>
      <% end %>
      </ul>

    <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
    <% end %>

    <%= yield %>

　  <footer>
      <p>DIVE INTO CODE 201808期生</p>
      <p>Copyright Takuya Nakahara</p> 
    </footer>

  </body>
</html>
